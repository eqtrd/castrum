name: Staging Deployment
on:
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 2
      - name: FTP Deployment
        uses: SamKirkland/FTP-Deploy-Action@4.3.3
        with:
          server: ftp.cluster027.hosting.ovh.net
          username: eliequinjn
          password: uDSSYpDrdcre
          protocol: ftp
          server-dir: github_deploy