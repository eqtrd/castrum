<?php

return [
   /* 'page.update:after' => function ($newPage, $oldPage) {
        $template = $newPage->coverimage()->toFile()->template();
        if ($template == ""): $template = "hello"; endif;
        throw new Exception($newPage->blueprint());
    },*/
];